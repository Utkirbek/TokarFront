import { createStyles } from "@mantine/styles";

export const useSpendStyles = createStyles(() => ({
  tr: {
    minWidth: 400,
    margin: "25px 0",
    textAlign: "center",
  },
  td: {
    width: "10%",
    padding: "5px",
    border: "1px solid",
  },
}));
